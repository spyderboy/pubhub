# ------------------------------------------------------------------------------
# PUBLISHER STACK ENVIRONMENT TEMPLATE
# Instructions: Copy this file to .env and fill in your keys.
# Note: Backend secrets should be set via Firebase CLI (see README).
# ------------------------------------------------------------------------------

# --- FRONTEND (Flutter) ---
# Found in Firebase Console > Project Settings
FLUTTER_APP_FIREBASE_API_KEY="your-api-key-here"
FLUTTER_APP_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"
FLUTTER_APP_FIREBASE_PROJECT_ID="your-project-id"
FLUTTER_APP_FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"
FLUTTER_APP_FIREBASE_MESSAGING_SENDER_ID="your-sender-id"
FLUTTER_APP_FIREBASE_APP_ID="your-app-id"

# --- BACKEND (GCP Functions) ---
# The Resend Key is handled as a "Secret" in GCP. 
# This entry is for local testing documentation only.
RESEND_API_KEY="re_123456789abcdef"

# --- DEPLOYMENT (Netlify) ---
NETLIFY_SITE_ID="your-netlify-site-id"
